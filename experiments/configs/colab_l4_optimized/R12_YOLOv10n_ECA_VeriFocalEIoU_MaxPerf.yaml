# R12: YOLOv10n + ECA + VeriFocal+EIoU - Maximum Performance
# ============================================================================
# Target: 97.0% mAP@0.5 (YOLOv10n + ECA peak performance)
# Strategy: Architecture + channel attention + ultimate loss synergy

experiment:
  name: "R12_YOLOv10n_ECA_VeriFocalEIoU_MaxPerf_640px"
  type: "eca_attention_v10_peak_performance"
  mode: "train"
  description: "YOLOv10n with ECA attention + VeriFocal+EIoU optimized for maximum performance"
  tags:
    - "research_attention_enhancement"
    - "yolov10n"
    - "eca_attention"
    - "verifocal_eiou_loss"
    - "max_performance"
    - "phase_3"

model:
  type: "yolov10n"
  config_path: "ultralytics/cfg/models/v10/yolov10n-eca-research-optimal.yaml"
  pretrained: true
  attention_mechanism: "eca"

data:
  num_classes: 6

training:
  dataset:
    path: "experiments/configs/datasets/hripcb_data.yaml"
  epochs: 150                           # Optimized for performance vs time
  batch: 48                             # Conservative for YOLOv10n + ECA attention
  imgsz: 640
  device: "0"
  workers: 8                            # Colab CPU optimized
  seed: 42
  
  # YOLOv10n + ECA attention-optimized hyperparameters for maximum performance
  optimizer: "AdamW"
  lr0: 0.0007                           # Conservative for YOLOv10n + attention
  lrf: 0.002                            # Lower final LR for attention fine-tuning
  weight_decay: 0.0001                  # Lower for better generalization
  momentum: 0.95                        # Higher momentum for smoother convergence
  warmup_epochs: 22.0                   # Extended warmup for YOLOv10n attention adaptation
  patience: 45                          # Higher patience for peak performance
  
  # Speed optimizations (non-performance affecting)
  save_period: 15                       # More frequent saves for peak model
  validate: true
  cache: true                           # Dataset caching for speed
  amp: true                            # Mixed precision (2x speed, no perf loss)
  project: "experiments/pcb-defect-research-maxperf"
  
  # VeriFocal+EIoU optimized for YOLOv10n + ECA
  loss:
    type: "verifocal_eiou"
    box_weight: 9.2                     # Enhanced for YOLOv10n + ECA precision
    cls_weight: 0.9                     # Maximum for VeriFocal + attention
    dfl_weight: 2.1                     # Enhanced DFL for YOLOv10n
    
  # Carefully balanced augmentation for YOLOv10n + ECA
  augmentation:
    mosaic: 0.7                         # Moderate mosaic
    mixup: 0.08                         # Minimal mixup
    copy_paste: 0.15                    # Conservative copy-paste
    hsv_h: 0.008                        # Very minimal color variations
    hsv_s: 0.35                         # Conservative saturation
    hsv_v: 0.18                         # Conservative brightness
    degrees: 0.0
    translate: 0.04                     # Minimal translation
    scale: 0.25                         # Conservative scaling
    shear: 0.0
    perspective: 0.0
    flipud: 0.0
    fliplr: 0.35                        # Conservative horizontal flip

validation:
  batch: 48                             # Match training batch for consistency
  imgsz: 640
  conf_threshold: 0.001
  iou_threshold: 0.6
  max_detections: 300
  split: "val"

wandb:
  project: "pcb-defect-research-maxperf"
  save_code: true
  dir: "./wandb_logs"

metadata:
  dataset_name: "HRIPCB"
  hardware: "Colab L4 GPU (22GB)"
  target_performance: "97.0% mAP@0.5"
  estimated_time: "2.5 hours"
  notes: "YOLOv10n architecture + ECA channel attention + VeriFocal+EIoU ultimate loss combination"