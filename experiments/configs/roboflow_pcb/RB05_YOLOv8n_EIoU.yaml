# RB05: YOLOv8n + EIoU Loss Only - Roboflow PCB Dataset
# ============================================================================
# Target: Evaluate EIoU loss function impact without attention mechanisms

experiment:
  name: "RB05_YOLOv8n_EIoU_Roboflow_640px"
  type: "roboflow_pcb_training"
  mode: "train"
  description: "YOLOv8n with EIoU loss only on Roboflow PCB dataset"
  tags:
    - "roboflow_pcb"
    - "yolov8n"
    - "eiou_loss"
    - "loss_ablation"

model:
  type: "yolov8n"
  config_path: "ultralytics/cfg/models/v8/yolov8n.yaml"
  pretrained: true
  attention_mechanism: null

data:
  path: "experiments/configs/datasets/roboflow_pcb_data.yaml"
  num_classes: 6

training:
  dataset:
    path: "experiments/configs/datasets/roboflow_pcb_data.yaml"
    num_classes: 6

  epochs: 150
  batch: 64
  imgsz: 640
  device: "0"
  workers: 16
  seed: 42
  
  # EIoU optimized hyperparameters
  optimizer: "AdamW"
  lr0: 0.001
  lrf: 0.01
  weight_decay: 0.0005
  momentum: 0.937
  warmup_epochs: 3.5
  patience: 32
  
  save_period: 20
  validate: true
  cache: true
  amp: true
  project: "experiments/roboflow-pcb-training"
  
  # EIoU loss configuration
  loss:
    type: "eiou"
    box_weight: 8.0                     # High precision focus
    cls_weight: 0.5
    dfl_weight: 1.7
    
  # Standard augmentation for fair comparison
  augmentation:
    mosaic: 1.0
    mixup: 0.1
    copy_paste: 0.3
    hsv_h: 0.015
    hsv_s: 0.7
    hsv_v: 0.4
    degrees: 0.0
    translate: 0.1
    scale: 0.5
    shear: 0.0
    perspective: 0.0
    flipud: 0.0
    fliplr: 0.5

validation:
  batch: 64
  imgsz: 640
  conf_threshold: 0.001
  iou_threshold: 0.6
  max_detections: 300
  split: "val"

wandb:
  project: "roboflow-pcb-training"
  save_code: true
  dir: "./wandb_logs"

metadata:
  dataset_name: "Roboflow PCB Defects"
  hardware: "GPU"
  target_performance: "Evaluate EIoU loss impact"
  notes: "YOLOv8n with EIoU loss only for loss function ablation study"